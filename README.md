# BookStore Frontend

This is the Angular frontend for the BookStore application.

## How to Run

1.  **Install Dependencies**:

    ```bash
    npm install
    ```

2.  **Run the Application**:

    ```bash
    npm start
    ```

    Navigate to `http://localhost:4200/`.

3.  **Build**:
    ```bash
    npm run build
    ```
